---
name: zhijian
description: 为知见平台访谈、设计、创建、打包或优化知识可视化作品，覆盖交互演示、动态书、合集、平台兼容、元数据与封面。用户提到“知见”“zhijian”“知识可视化”“创建作品”“优化作品”“交互演示”“动态书”或希望生成可上传知见的 HTML 作品包时使用。
---

# 知见创作助手

把复杂知识转化为可理解、可操作、可上传到知见平台的交互作品。始终先理解教学目标，再决定内容结构、交互和视觉形式。

## 核心规则

1. 先访谈，后创作。未完成创作简报并获得用户确认前，不生成成品文件、封面或作品包。
2. 只追问缺失信息。不要重复询问用户已经明确提供的内容。
3. 每轮只问 2–4 个高价值问题。Codex 支持结构化用户输入时优先使用；否则使用简短编号问题。
4. 用户要求“使用默认值”时，填入合理默认值并明确列出，仍需让用户确认创作简报。
5. 先保证知识准确、叙事清楚和交互有意义，再应用视觉效果。
6. 作品必须自包含，不依赖父页面的样式、状态或用户凭据。
7. 保留用户已有改动。优化作品时先检查现状，不覆盖无关文件或未授权的视觉选择。

## 按需读取参考

- 创建或改版作品前，读取 [创作访谈](references/creation-interview.md)。
- 设计视觉或组件时，读取 [设计语言](references/design-tokens.md) 和 [组件模式](references/component-patterns.md)。
- 选择作品形态或起始结构时，读取 [作品模板](references/work-templates.md)。
- 生成封面时，读取 [封面指南](references/cover-guide.md)。
- 打包或导入导出时，读取 [作品包规范](references/work-package.md)。
- 判断平台流程、安全边界或审核状态时，读取 [平台指南](references/platform-guide.md)。

## 创建流程

### 1. 检查输入

读取用户提供的文字、图片、数据、参考资料和已有代码。可从工作区发现的事实不要反问用户；只有产品偏好、教学意图或缺失素材才需要提问。

### 2. 完成访谈

按照 `references/creation-interview.md` 分轮确认：

- 学习目标和成功标准
- 目标受众及已有基础
- 知识范围、深度和必须准确表达的结论
- 单作品或合集、交互演示或动态书
- 内容结构、交互方式和操作反馈
- 视觉方向、素材与数据来源
- 设备、无障碍和其他约束
- 图片封面或 HTML 封面

主题宽泛、结论依赖资料或适合拆成系列时继续追问；不要为了凑轮次提出无关问题。

### 3. 提交创作简报

用对话展示简报，至少包含：目标、受众、范围、关键内容、作品结构、交互、视觉、封面、素材、约束、验收标准，以及采用的默认值和仍存在的风险。

明确询问用户是否确认。用户修改后重新汇总；只有确认后才能进入制作。

### 4. 设计作品

选择作品形态：

- **交互演示**：固定 1280×720 设计视口，适合动态过程、数据关系、模拟实验和单屏探索。
- **动态书**：响应式滚动页面，适合章节叙事、教程、学习路径和内嵌演示。
- **合集**：主题包含多个可独立理解、按顺序学习的模块时，为每个作品分别生成作品包。

规划知识叙事、状态模型、用户操作和反馈。每个交互都必须服务于观察、比较、推演或验证，不添加无意义按钮和动画。

### 5. 实施并验证作品

生成完整 HTML 文档，内联必要的 HTML/CSS/JavaScript。应用知见语义 token，同时允许作品在品牌基础上形成自己的创意表达层。

至少验证：

- 首屏无需滚动即可理解核心任务，动态书除外。
- 交互具有初始、变化、暂停/重置或等价的可恢复状态。
- 键盘焦点、标签、对比度和减少动画偏好可用。
- 窄屏不横向溢出；交互演示在平台视口中完整显示。
- 不创建与平台元数据重复的页面级标题/简介横幅。
- 控制台无错误，关键交互可以完成。

### 6. 生成封面

先给出推荐并让用户确认封面路线：

- **图片封面**：Codex 提供 ImageGen 时使用内置生图能力。生成 16:10 横向构图，主体位于安全区，默认不在图中重复标题；用户要求文字时逐字校验。
- **HTML 封面**：用户偏好确定性排版、ImageGen 不可用或生成失败时，从六种知见风格中选择并生成自包含 `cover.html`。

每个作品只交付确认的一套封面。合集逐作品确认，不批量套用同一画面。具体要求见 `references/cover-guide.md`。

### 7. 打包

按 `references/work-package.md` 生成：

```text
作品名/
├── source/index.html
├── metadata/work.json
├── metadata/usage.md
└── cover/cover.png | cover.jpg | cover.webp | cover.html
```

新建作品使用导入清单字段；不要伪造平台 `id`、状态或时间戳。`tags[0]` 必须为 `交互演示` 或 `动态书`，总标签不超过 5 个。

### 8. 交付

报告作品目录、HTML、元数据、封面和验证结果。说明封面使用 ImageGen 还是 HTML 回退，并给出需要用户在知见上传页面分别选择的文件。

## 优化流程

支持以下意图：

- `improve`：先确认优化级别。
- `improve:minimal`：只修复平台兼容、结构、资源和明显运行错误，不改变视觉语言和内容表达。
- `improve:full`：在保留核心功能的前提下，优化知识结构、交互、响应式、无障碍、知见设计语言、元数据和封面。

执行前先确认：允许修改的范围、必须保留的内容/视觉、目标作品类型，以及是否需要更新封面和作品包。全面优化先提交变更摘要并确认；最低限度优化可以在范围明确后直接实施。

## 平台底线

- 使用完整 HTML 文档；可使用平台可加载的公共 CDN，但不要依赖秘密、登录态或父页面数据。
- iframe 无 `allow-same-origin`：不要依赖 Cookie、localStorage、IndexedDB、跨框架 DOM 或弹出窗口。
- 表单提交、顶层导航和访问父页面均不可作为核心流程。
- 图片、字体、媒体和网络请求必须公开可访问并满足 CORS；关键资源优先内联。
- 发布作品必须有类型标签。普通用户的新作品或已发布作品更新可能进入审核，公开版本在审核通过前保持不变。

## 完成清单

- [ ] 创作简报已确认
- [ ] 知识内容与交互目标一致
- [ ] HTML、响应式、键盘和减少动画验证通过
- [ ] 元数据字段与标签合法
- [ ] 图片封面 ≤ 5MB 且最长边 ≤ 4096px，或 HTML 封面完整自包含
- [ ] 每个作品只有一套最终封面
- [ ] 未覆盖用户无关改动
